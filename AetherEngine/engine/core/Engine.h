#pragma once

// Core engine class for the Aether Engine.
namespace aether
{
	// Main engine class responsible for initializing and running the engine.
	// The engine is a type, not a namespace dump.
	class Engine
	{
		public:
			Engine();
			void Run();
	};
}